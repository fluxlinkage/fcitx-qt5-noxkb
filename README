Fcitx Qt5 Input Context

This fork does not use xkbcommon for compose. It could be useful if you want to use fcitx in Qt >= 5.5 on old systems which do not have libxkbcommon >= 0.5.0 (e.g. Debian 8). 
